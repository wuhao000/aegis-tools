{
  "name": "aegis-ui-tools",
  "version": "1.0.7",
  "description": "前端项目辅助工具",
  "author": "wuhao",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p src",
    "publish": "npm run build",
    "test": "npm run build && node ./src/generate-api.js"
  },
  "repository": "https://github.com/wuhao000/aegis-tools.git",
  "devDependencies": {
    "aegis-ui-desktop": "^1.0.3",
    "tslib": "^1.10.0",
    "typescript": "^3.5.2"
  },
  "files": [
    "src"
  ],
  "publishConfig": {
    "registry": "http://registry.npmjs.org"
  },
  "dependencies": {
    "@types/node": "^12.0.10",
    "axios": "^0.19.0",
    "fs": "0.0.1-security",
    "js-beautify": "^1.10.0",
    "lodash.merge": "^4.6.2"
  },
  "bin": {
    "generate-api": "./src/generate-api.js"
  }
}
